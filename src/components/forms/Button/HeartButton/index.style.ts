import { motion } from "framer-motion";

import styled from "@emotion/styled";

export const HeartWrapper = styled(motion.div)`
    cursor: pointer;
`;

export const HeartIconWrapper = styled(motion.div)``;
